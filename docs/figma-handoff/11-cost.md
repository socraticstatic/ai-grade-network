# 11 — Cost

Route `/naas/cost`, meridian estate. Artboard 1440×1582. 123 measured
elements — full boxes in `captures/11-cost/measure.json`; ground truth
`captures/11-cost/11-cost@2x.png`; importable master
`artboards/11-cost.html` (fonts embedded).

State shown: Cost verb page — savings-led framing ("Save"-first labels;
"Cost" only where the figure is literal spend), egress recovery figures.
Drift fixed (§11): page title stock text-xl/600 → text-figma-lg bold
(ramp step + weight). Vocabulary audit: labels already lead with savings —
no copy drift.

Chrome: header + rail map to `Header/MainNav` / `Nav/LeftRail`
components (see 13-components.md).
