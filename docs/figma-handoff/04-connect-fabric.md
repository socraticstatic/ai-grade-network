# 04 — Connect · Fabric overview

Route `/naas/connect`, meridian estate. Artboard 1440×1486. 117 measured
elements — full boxes in `captures/04-connect-fabric/measure.json`; ground truth
`captures/04-connect-fabric/04-connect-fabric@2x.png`; importable master
`artboards/04-connect-fabric.html` (fonts embedded).

State shown: FabricHero resting — left column site classes (Data centers /
Offices / Branches / ATMs with on-fabric counts), AT&T Fabric column
("see inside"), right column 8 cloud regions + Internet/SaaS breakout;
solid edges = private (NetBond/DX labels), dashed = public. "All 4 types"
class cards + search/sort; AT&T Fabric summary (1 dual path, 2
cloud-to-cloud); Connections list (us-east-1 Dual 3ms, South Central US
Single 9ms + Make dual).

All SVG — imports as editable vectors. No drift found.

Chrome: header + rail map to `Header/MainNav` / `Nav/LeftRail`
components (see 13-components.md).
