# 16 — AI · Providers

Route `/ai/providers`, meridian estate. Artboard 1440×965. 142 measured
elements — full boxes in `captures/16-ai-providers/measure.json`; ground truth
`captures/16-ai-providers/16-ai-providers@2x.png`; importable master
`artboards/16-ai-providers.html` (fonts embedded).

Every model endpoint the gateway can route to — the AI layer's Connect
surface. Model catalog table (model, kind, endpoint, P50, price, state)
with helion-70b on CoreWeave, helion-cls-13b air-gapped on Nebius, and
GPT-class external on OpenAI, all "Not attached". Header states the
governance premise: attaching governs what comes next, not what already
went.

Audit: CLEAN — no type off the rendered ramp, no spacing off the 4px grid,
no colour outside tokens.json plus the sanctioned fw overrides.

Layers arrive named from the freeze (see METHOD.md §"Naming layers at the
source"): content names clipped at 44 characters, else the established
role vocabulary — Row, Stack, Grid, Card, Section, Flex item, Hover group,
Rule. Chrome maps to `Header/MainNav` / `Nav/LeftRail` on board 13.
