{
  "name": "ai-grade-network",
  "version": "1.0.0",
  "private": true,
  "description": "AT&T AI-grade network - NaaS storefront. Static site, no build step: npm start serves it.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "http-server . -p 8787 -c-1",
    "test": "node --test tests/*.test.mjs",
    "storefront": "http-server . -p 8787 -c-1"
  },
  "devDependencies": {
    "http-server": "14.1.1"
  }
}
