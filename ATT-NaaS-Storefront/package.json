{
  "name": "att-naas-storefront",
  "version": "1.0.0",
  "private": true,
  "description": "AT&T AI-grade network - NaaS storefront prototype. Static site: no build step. `npm start` serves it.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "serve -l 8080 --no-clipboard .",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
